# ContentLoop 开发代理协作规范

本文件约束所有在本仓库中工作的开发代理。目标是让后续改动延续同一套产品事实、数据边界和质量标准，而不是让每次会话重新猜测项目。

## 1. 事实来源与优先级

发生冲突时，按以下顺序判断：

1. 用户在当前任务中的明确要求；
2. 当前可运行代码、自动测试和部署配置；
3. `design-system/MASTER.md` 中仍适用的视觉与交互规范；
4. `docs/product/` 中的 PRD、Schema、验收记录和历史决策；
5. Obsidian 知识库中的长期记录。

`docs/product/` 和 Obsidian 用来解释背景与决策，不保证始终领先于代码。发现过时描述时应修正文档，不能为了符合旧文档而回退已确认能力。不要在文档中硬编码“当前 Phase”或发布版本号，除非任务明确要求维护版本记录。

## 2. 产品定位与当前边界

ContentLoop 是面向游戏内容运营与图文创作者的 AI 增长工作台，当前首个完整场景是《金铲铲之战》。核心链路为：

```text
输入灵感 / 公开网页 / 截图
→ AI 理解与事实边界
→ 5 个候选选题
→ 研究简报与 3 个观点
→ 严格七页大纲
→ 3:4 图文编辑与导出
→ 发布文案与可选 Obsidian 写回
```

当前产品没有用户账号、团队空间、云端草稿同步、自动发布、商业计费后台，也不保证所有游戏数据实时准确。不要在实现或文档中暗示这些能力已经存在。

## 3. 架构职责

| 区域 | 主要职责 | 修改注意事项 |
| --- | --- | --- |
| `src/app.js` | 五阶段路由、表单事件、状态编排和 UI | 避免继续堆放可独立测试的领域逻辑 |
| `src/domain/schema.js` | 项目数据结构与规范化 | 结构变化必须考虑旧本地草稿迁移 |
| `src/domain/state.js` | 路由与状态变更 | 只允许 `home`、`research`、`outline`、`editor`、`export` 五个阶段 |
| `src/domain/ai-contract.js` | AI 任务协议、白名单和结果校验 | 客户端、服务端、模拟提供器必须同步更新 |
| `src/domain/history.js` | 撤销、重做、保存版本和恢复 | 不得把访问码、密钥或图片二进制写入历史 |
| `src/domain/dynamic-pages.js` | 七页内容结构与动态页面 | 不得恢复固定英雄数或占位式重复文案 |
| `src/domain/canvas-renderer.js` | 1080×1440 预览与导出渲染 | 预览与导出必须使用同一渲染模型 |
| `src/domain/export-quality.js` | 溢出、缺图和结构检查 | 不能通过隐藏或截断正文来“通过检查” |
| `src/services/` | 浏览器端网络、OCR、视觉和 Obsidian 适配 | 网络失败必须保留用户已有内容 |
| `scripts/` | 本地服务、AI 提示词、来源提取、构建和检查 | 不得向客户端注入服务端密钥 |
| `worker/index.js` | 线上 API 入口与静态站点 | 新接口同时考虑鉴权、限流、超时与错误格式 |
| `tests/` | 核心行为回归 | 修复线上问题时优先补对应回归测试 |

## 4. 不可破坏的产品规则

### 4.1 人工修改优先

- 用户手动修改的标题、页面说明、要点、排序和发布文案不得被静默覆盖。
- 锁定页面、锁定字段和“保留”要求必须跨 AI 重写、步骤切换和重新渲染生效。
- 第四步必须读取第三步当前已保存的大纲，不能读取第一次生成时的旧快照。
- AI 请求失败时保留旧内容；禁止用模板结果覆盖后再提示失败。
- 撤销、重做和历史恢复后，页面预览必须与项目状态一致。

### 4.2 七页结构与动态内容

- 完成大纲后应保持严格七页；页码、导出文件和发布包顺序一致。
- 七页是结构约束，不代表七页套用同一段句式。标题、正文、结论和卡片说明要根据真实页面任务分别写。
- 英雄、装备、羁绊和条目数量来自当前内容。不得为了排版整齐虚构条目或强制填满固定槽位。
- 单英雄页面应让主体充满图片区域；多英雄首页允许有层次的非机械排列和整体虚化背景。
- 对事实型素材使用已解析的真实 ID；模型不能新增不存在的英雄、装备、羁绊或来源引用。

### 4.3 事实与来源边界

- 不虚构胜率、登场率、版本强度、发布日期、个人实测或“官方结论”。
- 演示榜单和示例阵容必须明确标为非实时、演示或待核验。
- AI 只能使用请求中的 `allowedFactIds`、`allowedSourceIds` 和 `allowedAssetIds`。
- 非官方网页链接仅作用户内部参考，不在最终作品中伪装成官方来源。
- 无法读取网页正文时明确返回失败或要求手动粘贴，禁止根据标题和 URL 猜内容。

### 4.4 文字必须完整可读

- 页面正文、黄色提示框和最后一页清单不能被裁切、遮挡或用省略号掩盖。
- 同类正文使用统一字号，常规正文不得低于设计系统规定的可读下限。
- 文本框应按内容自适应，或让所在卡片组按最大内容统一高度；不得让编号图标覆盖标题或正文。
- 中文标点不能单独落在新行；清理模型输出中的字面量 `\\n`、Markdown 代码围栏和不应展示的 JSON。
- 内容超过安全容量时优先精简表达、重排版或明确提示用户，不能静默丢字。

## 5. AI 接入规范

当前支持六种任务：

```text
understand_input
extract_source
build_research_brief
generate_outline
rewrite_fields
generate_publish_copy
```

新增或修改 AI 能力时，依次检查：

1. `src/domain/ai-contract.js` 中的任务名、请求结构、响应结构和字段白名单；
2. `scripts/ai-service.mjs` 中的系统指令、任务指令、结果 Schema、解析与重试；
3. `worker/index.js` 与本地服务是否使用同一调用约定；
4. `src/services/ai-client.js` 的超时、取消、错误显示和访问码传递；
5. `src/domain/mock-ai-provider.js` 的无密钥演示行为；
6. `src/app.js` 中加载态、禁用态、成功写入和失败回滚；
7. `tests/ai-contract.test.mjs`、`tests/ai-server.test.mjs` 及相关流程测试。

AI 实现必须遵守：

- API Key 只从服务端环境变量读取，不出现在 HTML、客户端 bundle、日志或 Git 中。
- 访问码通过请求头发送，只保存在 `sessionStorage`；不写入 `localStorage`、历史或 Obsidian。
- 一次用户动作只发起一个上游模型请求。流式心跳用于保持连接，不能变成重复调用。
- 返回结果先清除代码围栏和包裹文本，再解析 JSON，并经过契约验证后写入状态。
- 只对可恢复的网络或格式异常做有限重试；当前链路至多追加一次自动重试。
- 超时、取消、鉴权、限流、JSON 错误和模型校验错误要向用户显示不同且可理解的信息。
- 模拟提供器只用于演示和测试，界面不得把模拟结果标成“在线模型已生成”。
- 优化速度时先减少上下文、输出长度和重复计算，不得移除事实校验或缩短到不完整内容。

## 6. 网页提取与服务端安全

- 只允许公开的 `http:` 和 `https:` URL。
- 拒绝 localhost、环回、链路本地、私网 IP、带用户名密码的 URL 和内部主机名。
- 保留响应体上限、超时和重定向后的地址校验；不能只校验初始 URL。
- 直接读取失败时可使用既有 Reader 降级，但仍要把来源状态传给前端。
- 网页提取接口与 AI 接口统一返回结构化错误，不能把 HTML 错误页交给 JSON 解析器。

## 7. 截图 OCR 与视觉识别

- 默认使用浏览器本地 Tesseract.js；原图不落盘、不写入项目草稿。
- 识别中文大标题、分组结构、排名和小字时保留区域信息，低置信度内容进入人工校对。
- 不根据头像猜英雄名称，不把“待核对”占位文本当成已确认正文。
- 只有用户明确同意后才能调用在线视觉识别；界面要说明图片将发送给模型并可能产生费用。
- 视觉请求使用访问码、大小限制、取消信号和超时；网络失败时保留本地 OCR 与用户编辑结果。
- 用户确认按钮应根据“存在可编辑识别文本”判断，不应因为置信度低或仍有待核对行而永久禁用。
- 已确认截图必须提供明确删除入口，并同步清理该截图对应的项目引用。

## 8. 状态、存储与历史

- 项目草稿存入 `localStorage`，AI 访问码存入 `sessionStorage`，Obsidian 目录句柄存入 `IndexedDB`。
- 改 Schema 时为旧草稿提供规范化默认值；不要用清空用户本地数据解决兼容问题。
- 历史最多保留 20 个自动版本；命名版本、删除与恢复行为应保持可预测。
- 大图片、OCR 语言包和其他二进制内容不能写入项目 JSON 或历史栈。
- 从步骤 A 进入步骤 B 前，先把当前编辑框内容提交到唯一项目状态，避免 UI 与预览使用不同副本。

## 9. Obsidian 运行时写回

网站连接 Obsidian 时：

- 仅支持用户通过目录选择器主动授权的本地 Vault；
- 连接前验证目录含 `.obsidian`；
- 只写入 `AI待确认/铲友创作台自动同步/当前草稿.md` 和 `同步记录.md`；
- 不扫描、上传或重写 Vault 其他内容；
- 不写密钥、访问码、认证头、完整原始截图或隐私信息；
- 权限被撤销或浏览器不支持时，给出明确说明，不能显示“已同步”。

运行时写回与开发记录同步是两件事。前者由网站在用户浏览器中执行；后者由开发代理在完成实质改动后维护。

## 10. 视觉、交互与导出

以 `design-system/MASTER.md` 为视觉基线：暗紫背景、暖金强调、系统中文字体、清晰的编辑态与状态文本。

- 交互目标最小 44×44px，所有主要操作可用键盘完成。
- 不能只靠颜色表达成功、警告、锁定或错误；同时提供图标或文字。
- 保持 375px 移动端可用，并尊重 `prefers-reduced-motion`。
- 不用 emoji 充当产品图标；使用既有 SVG/图标体系。
- Riot 原始游戏图标不得做滤镜、改色或改变角色身份的生成式编辑。
- 预览和导出共用 `canvas-renderer.js`，避免“页面看到的”和“下载得到的”不一致。
- 1080×1440 是最终画布尺寸；CSS 预览缩放不能改变布局计算基准。
- 修改排版后至少覆盖：单英雄、多英雄、长标题、长正文、四卡片、最后一页清单和黄色提示框。

## 11. 验证要求

基础命令：

```bash
npm run check
npm test
npm run build
```

按改动范围选择验证：

| 改动类型 | 最低验证 |
| --- | --- |
| 文档 | 检查链接、命令、路径和事实与代码一致；建议运行 `npm run check` |
| UI/CSS | `npm run check`、相关测试、目标桌面与 375px 布局检查 |
| 状态/历史/Schema | `npm run check`、`npm test`、旧草稿兼容检查 |
| AI/来源/OCR | `npm run check`、`npm test`、成功/超时/鉴权/无效 JSON/取消流程 |
| Canvas/导出 | `npm test`、长短内容样例、预览与导出一致性检查 |
| 构建/Worker/部署 | 全部三条命令，本地 API 与静态路由检查 |

不要只验证“按钮能点”。至少验证结果真实写入正确状态、切换步骤后仍然存在、失败时旧内容没有丢失。

修复线上缺陷时，应先用失败场景补回归测试，再修复实现。若某项无法自动化，交付时说明手动验证方法和残余风险。

## 12. Git、发布与敏感信息

- 保留用户已有改动，不使用 `git reset --hard`、`git checkout --` 或批量覆盖解决冲突。
- 提交前检查 `git diff`，避免把临时截图、构建产物、日志和本机路径误提交。
- `.env*`、`.dev.vars*`、API Key、访问码、Cookie、令牌和部署凭证绝不能进入仓库。
- 当前 GitHub 远端为 `origin`，Sites 远端为 `sites`；推送或发布前确认目标分支与用户授权。
- 文档或代码修改不等于自动部署。只有用户明确要求发布时才执行 Sites 发布流程。
- 发布前必须完成 `npm run check`、`npm test` 和 `npm run build`，并记录线上 URL、版本说明和验证结果。
- 不把 Sites OAuth URL、一次性发布令牌或其他凭证写进聊天总结、文档、提交信息和 Obsidian。
- 当前仓库未附开源许可证；不得在 README 或页面中声称可自由复制、修改或商用。

## 13. 开发知识库持续同步

长期知识库位于：

```text
C:/Users/HUAWEI/Documents/Codex/游戏内容创作知识库
```

完成实质功能、缺陷修复、架构决定、发布或重要验证时：

1. 写入前读取 Vault 根目录 `AGENTS.md`；
2. 读取 `产品探索/知识库持续同步规则.md`；
3. 将开发决策与验证结果写入 `产品探索/`，包含日期、改动范围、关键文件、验证和遗留风险；
4. AI 生成的游戏知识、选题和未验收材料写入 `AI待确认/`，不能直接覆盖正式知识；
5. 读回所写文件确认成功；失败时写入仓库 `pending-obsidian-sync/` 并在交付中说明；
6. 不记录任何密钥、访问码、认证头、个人隐私或高分辨率原始截图。

知识库是决策与经验的索引，不是源代码镜像。不要把 README、源码或整个项目逐字复制到 Obsidian；记录“为什么改、改了什么、如何验证、下一步风险”即可。

## 14. 完成定义

一个任务只有同时满足以下条件才算完成：

- 用户要求的行为已实现，而不只是新增按钮、提示或演示数据；
- 人工编辑、锁定字段和旧内容在成功与失败路径上都得到保护；
- 新增 AI/OCR/来源数据通过结构与事实边界校验；
- 相关测试通过，必要时完成完整 check、test、build；
- 文档、示例和界面描述没有夸大当前能力；
- 实质改动已按规则同步到 Obsidian 或明确记录待同步；
- 交付说明包含结果、验证、未完成事项和是否已发布。
