# 项目协作规则

- 产品事实以 `docs/product/` 中的 PRD、Schema 和任务拆分为准。
- 当前阶段是 Phase B：七页编辑器与版本保护。
- 不把演示数据写成实时数据，不虚构胜率、版本结论或个人实测。
- 用户手改字段、锁定页面和历史版本不得被静默覆盖。
- 保持无账号、无 API、无 Obsidian 连接时也能完整体验样例。
- 交互必须可用键盘操作，触控目标不小于 44px，颜色状态同时带文字。
- 不新增付费服务、外部部署或破坏性数据操作，除非用户明确授权。

## 知识库持续同步（用户于 2026-09-05 确认）

- 本项目长期知识库：`C:/Users/HUAWEI/Documents/Codex/游戏内容创作知识库`。写入前读取该 Vault 的 `AGENTS.md` 和 `产品探索/知识库持续同步规则.md`。
- 每次实质改动、需求决定、发布或验证结束前，记录到 Vault 的 `产品探索` 并读回核验；同步失败则留 `pending-obsidian-sync/` 待办并向用户说明，不能声称已同步。
- AI 生成的游戏知识、选题和未验收材料先入 `AI待确认`。不静默覆盖正式事实；不记录访问码、密钥或认证头。
- 开发记录同步不等于网站自动写回 Obsidian；未实现运行时接入前不得宣称已接通。
